const path = require('path');

const config = {
  // create entry point
  //TODO

  // define output file(s)
  //TODO

  // define module loaders which preprocess src files
  // handle .ts, .css, .sass, images etc.
  // loaders enhance webpack, are separate libs
  //TODO

  // only discover files that have those extensions
  //TODO
};

module.exports = config;
